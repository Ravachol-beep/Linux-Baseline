# Linux-Baseline
cis-baseline is a standalone Bash script that audits a Linux system against the Center for Internet Security (CIS) Benchmark guidelines for Level 1 and Level 2 hardening. It checks ~70 controls across filesystem configuration, network settings, services, logging, SSH, PAM, and user account policies.
